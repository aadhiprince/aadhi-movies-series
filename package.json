{
  "name": "aadhi-movies-series",
  "version": "1.0.0",
  "description": "Movies & Series Watchlog Web App",
  "main": "backend/server.js",
  "scripts": {
    "postinstall": "cd backend && npm install",
    "build": "cd backend && npm install",
    "start": "cd backend && node server.js"
  }
}
